---
name: nuxt-data-layer
description: How this project talks to its API — the two-layer service + Pinia Colada contract, query keys, cache invalidation, response schema parsing, the fetcher, and central error handling. Use when adding or changing an API endpoint, wiring a component to server data, writing a query or mutation composable, deciding where a side effect belongs, or debugging a failed request, a stale cache, or an unexpected toast.
---

<!-- catalyst:generated skill wrapper — the substance lives in the catalyst/ documents below; edit those, never this file. Regenerated on spawn and on every upgrade apply. -->

Read and follow, in order:

- `catalyst/stacks/frontend/nuxt/data-layer.md`
- `catalyst/stacks/frontend/nuxt/error-handling.md`

Paths inside those documents are relative to `catalyst/`. Repo conventions there win over any generic guidance from similarly-named installed skills or plugins.
