---
name: vue-style
description: How this project writes Vue — the script section order, template rules, the auto-import boundary, event and handler naming, style blocks, and component file naming. Use when writing or changing a Vue SFC, adding a component, deciding where something belongs in the script block, naming an emit or its handler, or wondering whether a symbol needs importing. Complements audit-vue-style (which checks and fixes existing files) — this skill is the guide you write against.
---

<!-- catalyst:generated skill wrapper — the substance lives in the catalyst/ documents below; edit those, never this file. Regenerated on spawn and on every upgrade apply. -->

Read and follow, in order:

- `catalyst/stacks/frontend/_vue/vue-style.md`
- `catalyst/stacks/frontend/_vue/vue-style-examples.md`
- `catalyst/stacks/frontend/_vue/component-naming.md`
- `catalyst/stacks/frontend/_common/component-naming.md`

Paths inside those documents are relative to `catalyst/`. Repo conventions there win over any generic guidance from similarly-named installed skills or plugins.
