---
name: layout-stability
description: How a component keeps its size when its own state changes — reserving the tallest variant of state-dependent copy, fixed slots for values that grow a digit, rows that absorb an appearing badge, and when a block that unmounts should leave its space behind. Use when building or changing a component whose text, value, badge, or section varies by state, when a card resizes or the page jumps as something is toggled, or when reaching for a fixed height or an animation to stop that jump.
---

<!-- catalyst:generated skill wrapper — the substance lives in the catalyst/ documents below; edit those, never this file. Regenerated on spawn and on every upgrade apply. -->

Read and follow, in order:

- `catalyst/stacks/frontend/_common/layout-stability.md`

Paths inside those documents are relative to `catalyst/`. Repo conventions there win over any generic guidance from similarly-named installed skills or plugins.
