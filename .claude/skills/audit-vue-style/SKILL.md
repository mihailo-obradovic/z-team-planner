---
name: audit-vue-style
description: Audit Vue SFCs (and composable/util .ts files) for conformance to the project's Vue style guide, then apply fixes. Use whenever the user wants to review, audit, clean up, or check Vue components or pages for style, conventions, script section order, auto-import correctness, prop typing, template casing, event and handler naming, or accessibility — even phrased loosely as 'clean up this component' or 'does this follow our conventions'.
---

<!-- catalyst:generated skill wrapper — the substance lives in the catalyst/ documents below; edit those, never this file. Regenerated on spawn and on every upgrade apply. -->

**Target:** $ARGUMENTS (a file or directory to audit; when empty, the files changed on the current branch)

Read and follow, in order:

- `catalyst/stacks/frontend/_vue/style-audit.md`

Paths inside those documents are relative to `catalyst/`. Repo conventions there win over any generic guidance from similarly-named installed skills or plugins.
