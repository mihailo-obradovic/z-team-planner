---
name: nuxt-forms
description: How this project builds and validates forms — Regle rules mirroring the backend, Zod's response-only role, and the path that renders server 422s inline on the field instead of as a toast. Use when building or changing a form, adding validation, wiring a dialog form to a mutation, or fixing validation errors that appear in the wrong place or not at all.
---

<!-- catalyst:generated skill wrapper — the substance lives in the catalyst/ documents below; edit those, never this file. Regenerated on spawn and on every upgrade apply. -->

Read and follow, in order:

- `catalyst/stacks/frontend/nuxt/validation.md`
- `catalyst/stacks/frontend/nuxt/error-handling.md`

Paths inside those documents are relative to `catalyst/`. Repo conventions there win over any generic guidance from similarly-named installed skills or plugins.
