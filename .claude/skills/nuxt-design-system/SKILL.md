---
name: nuxt-design-system
description: The project's design system — semantic color tokens, the type scale with its line heights, spacing, sizing, radii and border widths, elevation, opacity, the z-index scale, iconography, imagery, and the per-element and layout values components are checked against. Use when styling anything, adding or changing a token, picking a size, shadow, or spacing value, building a new component, or instantiating the template into the project's design-system annex at Init Design.
---

<!-- catalyst:generated skill wrapper — the substance lives in the catalyst/ documents below; edit those, never this file. Regenerated on spawn and on every upgrade apply. -->

Read and follow, in order:

- `catalyst/stacks/frontend/nuxt/design-system.md`

Paths inside those documents are relative to `catalyst/`. Repo conventions there win over any generic guidance from similarly-named installed skills or plugins.
