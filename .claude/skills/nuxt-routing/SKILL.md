---
name: nuxt-routing
description: How this project routes — pages, layouts, route middleware as a thin adapter over a pure policy function, auth guards, redirects, and session priming under SPA rendering. Use when adding a page or layout, changing who can reach a route, debugging a redirect loop, or working on navigation or auth-driven route changes.
---

<!-- catalyst:generated skill wrapper — the substance lives in the catalyst/ documents below; edit those, never this file. Regenerated on spawn and on every upgrade apply. -->

Read and follow, in order:

- `catalyst/stacks/frontend/nuxt/routing.md`

Paths inside those documents are relative to `catalyst/`. Repo conventions there win over any generic guidance from similarly-named installed skills or plugins.
