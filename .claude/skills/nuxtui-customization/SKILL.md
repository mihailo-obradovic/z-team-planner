---
name: nuxtui-customization
description: How this project changes what a Nuxt UI component looks like — the three-file layout (`config/nuxt-ui/`, `app.config.ts`, `types/nuxt-ui.d.ts` under the app's srcDir), vendoring a component's complete upstream default theme rather than a delta, the annotations that keep every deviation recoverable, and the wiring rules a mismatched key fails silently on. Use when restyling a component, adding a variant or a colour, reviewing a config file, or re-importing defaults after an `@nuxt/ui` bump.
---

<!-- catalyst:generated skill wrapper — the substance lives in the catalyst/ documents below; edit those, never this file. Regenerated on spawn and on every upgrade apply. -->

Read and follow, in order:

- `catalyst/stacks/frontend/nuxt/ui/nuxtui/customization.md`
- `catalyst/stacks/frontend/nuxt/ui/nuxtui/nuxtui.md`

Paths inside those documents are relative to `catalyst/`. Repo conventions there win over any generic guidance from similarly-named installed skills or plugins.
