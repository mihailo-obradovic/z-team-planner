---
name: mold
description: Reshape verbose or agent-generated code to the molded standard — comments that earn their line by stating a why the code cannot express, self-describing names, one primary function per module, one-use helpers inlined and dead abstractions dissolved. Use when cleaning up freshly generated code, trimming narration comments, or writing a new component or module to the standard rather than cleaning it back to one.
---

<!-- catalyst:generated skill wrapper — the substance lives in the catalyst/ documents below; edit those, never this file. Regenerated on spawn and on every upgrade apply. -->

**Target:** $ARGUMENTS (files or directories to mold; when empty, the files changed on the current branch)

Read and follow, in order:

- `catalyst/conventions/mold.md`
- `catalyst/conventions/code-annotations.md`

Paths inside those documents are relative to `catalyst/`. Repo conventions there win over any generic guidance from similarly-named installed skills or plugins.
