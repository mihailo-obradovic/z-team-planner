minimumReleaseAge: 1440

# * Dependencies whose build scripts are deliberately skipped — none is needed (sharp ships prebuilt @img/* binaries). Listing them only silences the install warning; re-check against a fresh install when dependencies move.
ignoredBuiltDependencies:
  - '@firebase/util'
  - '@parcel/watcher'
  - esbuild
  - protobufjs
  - sharp
  - vue-demi
minimumReleaseAgeExclude:
  # Renovate security update: vitest@4.1.11
  - vitest@4.1.11
